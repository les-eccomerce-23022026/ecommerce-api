================================================================================
LOG DE EXECUÇÃO - Migration 012
================================================================================
Arquivo: 012_criar_tabelas_frete_entrega.sql
Data de execução: 2026-03-25
Banco de dados: ecm_livraria
Usuário: ecm_user
================================================================================

ALTERAÇÕES REALIZADAS:
----------------------

1. Tabela: tipo_frete
   - Criação: SUCESSO
   - Colunas: tfr_id (PK), tfr_descricao (UNIQUE), tfr_criado_em
   - Seeds inseridas: 3 registros (PAC, SEDEX, RETIRA_EM_LOJA)

2. Tabela: entrega
   - Criação: SUCESSO
   - Colunas: ent_id (PK), ent_uuid (UNIQUE), ven_id (FK), tfr_id (FK),
              ent_endereco_json, ent_custo, ent_entregador, ent_criado_em
   - Constraint CHECK: ent_custo >= 0
   - FK: referencia ecm_venda(ven_id) ON DELETE CASCADE
   - FK: referencia tipo_frete(tfr_id)

3. Índices criados:
   - idx_entrega_uuid (ent_uuid)
   - idx_entrega_venda (ven_id)
   - idx_entrega_tipo_frete (tfr_id)

================================================================================
CORREÇÕES APLICADAS (vs versão original):
================================================================================

[CORRIGIDO] Tabelas:
  - ecm_tipo_frete → tipo_frete (removeu prefixo ecm_)
  - ecm_entrega → entrega (removeu prefixo ecm_)

[AJUSTADO] Foreign Keys:
  - ecm_venda(ven_id) → mantido (aguarda correção da migration 010)
  - ecm_tipo_frete(tfr_id) → tipo_frete(tfr_id)

[ADICIONADO] Índice:
  - idx_entrega_tipo_frete para otimização de consultas por tipo de frete

[ADICIONADO] Comentários descritivos no SQL

================================================================================
VALIDAÇÃO:
================================================================================

Status: ✅ APROVADA

Tabelas criadas:
  ✅ tipo_frete (3 seeds inseridas)
  ✅ entrega (FKs válidas, índices criados)

Restrições verificadas:
  ✅ PKs com SERIAL e UUID
  ✅ FKs referenciando tabelas corretas
  ✅ CHECK constraint em ent_custo
  ✅ UNIQUE em ent_uuid e tfr_descricao
  ✅ ON DELETE CASCADE em ven_id

================================================================================
OBSERVAÇÕES:
================================================================================

NOTA: A FK para venda referencia temporariamente ecm_venda(ven_id) até que a
migration 010 seja corrigida para remover o prefixo ecm_ da tabela venda.

Ação futura necessária:
  - Corrigir migration 010 (criar_tabela_vendas.sql)
  - Renomear ecm_venda → venda
  - Atualizar esta migration para referenciar venda(ven_id)

================================================================================
